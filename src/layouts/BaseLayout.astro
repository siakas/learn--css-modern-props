---
import '../styles/globals.css'
import BackIndex from '../components/BackIndex.astro'
import Navigation from '../components/Navigation.astro'

interface Props {
  title?: string
  showBackIndex?: boolean
}

const { title = 'モダン CSS の学習', showBackIndex = true } = Astro.props
const year = new Date().getFullYear()
---

<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="robots" content="noindex,nofollow" />
    <title>{title}</title>
    <!-- ダークモード初期化（FOUC 防止のため head 内で同期実行） -->
    <script is:inline>
      const saved = localStorage.getItem('theme')
      const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches
      if (saved === 'dark' || (!saved && prefersDark)) {
        document.documentElement.classList.add('dark')
      }
    </script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:ital,wght@0,300;0,400;0,500;0,600;0,700;1,400&family=JetBrains+Mono:wght@400;500&display=swap"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.11.1/styles/a11y-dark.min.css"
    />
  </head>
  <body class="min-h-screen flex flex-col">
    <!-- ヘッダー -->
    <header class="site-header sticky top-0 z-50 border-b">
      <div class="mx-auto max-w-6xl px-8 h-16 flex items-center justify-between">
        <a href="/" class="site-logo font-bold text-xl no-underline hover:no-underline tracking-tight">
          CSS Modern Props
        </a>
        <button
          id="theme-toggle"
          type="button"
          aria-label="ダークモード切り替え"
          class="theme-toggle-btn rounded-lg p-2 transition-colors cursor-pointer"
        >
          <!-- 太陽アイコン（ダークモード時に表示） -->
          <svg
            class="sun-icon hidden size-5"
            xmlns="http://www.w3.org/2000/svg"
            fill="none"
            viewBox="0 0 24 24"
            stroke-width="1.5"
            stroke="currentColor"
            aria-hidden="true"
          >
            <path stroke-linecap="round" stroke-linejoin="round" d="M12 3v2.25m6.364.386-1.591 1.591M21 12h-2.25m-.386 6.364-1.591-1.591M12 18.75V21m-4.773-4.227-1.591 1.591M5.25 12H3m4.227-4.773L5.636 5.636M15.75 12a3.75 3.75 0 1 1-7.5 0 3.75 3.75 0 0 1 7.5 0Z" />
          </svg>
          <!-- 月アイコン（ライトモード時に表示） -->
          <svg
            class="moon-icon size-5"
            xmlns="http://www.w3.org/2000/svg"
            fill="none"
            viewBox="0 0 24 24"
            stroke-width="1.5"
            stroke="currentColor"
            aria-hidden="true"
          >
            <path stroke-linecap="round" stroke-linejoin="round" d="M21.752 15.002A9.72 9.72 0 0 1 18 15.75c-5.385 0-9.75-4.365-9.75-9.75 0-1.33.266-2.597.748-3.752A9.753 9.753 0 0 0 3 11.25C3 16.635 7.365 21 12.75 21a9.753 9.753 0 0 0 9.002-5.998Z" />
          </svg>
        </button>
      </div>
    </header>

    <!-- コンテンツエリア -->
    <div class="mx-auto w-full max-w-6xl px-8 py-12 flex-1">
      <div class="flex gap-14">
        <aside class="w-52 shrink-0">
          <Navigation />
        </aside>
        <main class="min-w-0 flex-1">
          <div class="prose prose-neutral max-w-none">
            <slot />
            {showBackIndex && <BackIndex />}
          </div>
        </main>
      </div>
    </div>

    <!-- フッター -->
    <footer class="site-footer border-t">
      <div class="mx-auto max-w-6xl px-8 py-6">
        <p class="footer-text text-sm text-center m-0">
          &copy; {year} Sanshiroh SAKAI. All rights reserved.
        </p>
      </div>
    </footer>

    <script
      src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.11.1/highlight.min.js"
    ></script>
    <script>
      declare const hljs: { highlightAll: () => void }
      hljs.highlightAll()
    </script>
    <script>
      const html = document.documentElement
      const toggle = document.getElementById('theme-toggle')
      const sunIcon = toggle?.querySelector<HTMLElement>('.sun-icon')
      const moonIcon = toggle?.querySelector<HTMLElement>('.moon-icon')

      function applyTheme(isDark: boolean) {
        html.classList.toggle('dark', isDark)
        sunIcon?.classList.toggle('hidden', !isDark)
        moonIcon?.classList.toggle('hidden', isDark)
      }

      // 初期状態を localStorage から復元してアイコン表示を同期
      const saved = localStorage.getItem('theme')
      const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches
      applyTheme(saved === 'dark' || (!saved && prefersDark))

      toggle?.addEventListener('click', () => {
        const isDark = !html.classList.contains('dark')
        applyTheme(isDark)
        localStorage.setItem('theme', isDark ? 'dark' : 'light')
      })
    </script>
  </body>
</html>

<style>
  .site-header {
    background-color: var(--background);
    border-color: var(--color-border);
  }

  .site-logo {
    color: var(--color-main);
  }

  .theme-toggle-btn {
    color: var(--muted-foreground);
  }

  .theme-toggle-btn:hover {
    background-color: var(--accent);
    color: var(--accent-foreground);
  }

  .site-footer {
    border-color: var(--color-border);
  }

  .footer-text {
    color: var(--muted-foreground);
  }
</style>
